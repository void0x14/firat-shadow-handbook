[package]
name = "firat-shadow-handbook"
version = "0.1.0"
edition = "2021"
description = "Fırat Üniversitesi için IT bağımsız shadow companion web uygulaması"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
chrono = { version = "0.4", features = ["serde"] }
rustls = "0.23"
webpki-roots = "0.26"
ring = "0.17"

[dev-dependencies]
httpmock = "0.6"
